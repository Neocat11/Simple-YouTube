{
  "name": "simple-youtube",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that hides YouTube thumbnails for a quieter browsing experience.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "vitest": "^4.1.5"
  }
}
